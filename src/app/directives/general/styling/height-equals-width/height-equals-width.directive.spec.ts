import { HeightEqualsWidthDirective } from './height-equals-width.directive';

describe('HeightEqualsWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new HeightEqualsWidthDirective();
    expect(directive).toBeTruthy();
  });
});
